# Stage 4 Gen II item-name checkpoint

Status: **independently verified: 253/253 tests passed, zero failures/errors/skips**. This completes the exact Japanese Gold/Silver-family and Crystal referenced item-name slice of Task 390 / Task 386 / `LNG-B002`, not Task 390 or Stage 4 as a whole. Parser revision is **57**; storage schema remains **2**.

## Static reference-scoped authority

Revision-56 Japanese Gold Rev 1 and Crystal controls retained numeric item references but no localized item names. The supported contract remains: publish a static label only for an independently authorized item reference, using the uniquely selected compiled consumer and bounded evaluation of that input. Original identity analysis freezes separate immutable Gen II compiled mechanics in the session; current accepted visible/hidden item extraction retains typed operand/map/handler provenance. The materializer consumes frozen authority without rediscovering or reselecting a consumer.

Gen II mechanics are independently bound, not imported from Gen I: a three-byte bank/pointer directory, `GetNthString(itemID - 1)`, and a compiled **11-byte copy**. Every requested start, terminator and full copy span must fit the bounded static bank mapping. Generated naming binds the banked helper, result in C, saved carry, skipped IDs C3/DC, TM-before-HM classification, compiled HM subtraction and owned stack/register/object restoration. Prefix lengths are **five bytes for TM and six for HM**; Japanese digits are fullwidth. Formatting hole inputs can duplicate numbers and does not prove those IDs are valid items.

Only current typed references receive labels. Readable unrequested rows, zero or arbitrary numeric queries gain no semantic authority. Exact codec tokens must terminate within proven bounds without invalid, control or substitution units. Malformed payloads fail locally without shrinking the expected reference denominator. Conflicting/incomplete consumers, budget exhaustion and cancellation fail closed. Global item counts, semantic whole-table extent and live mapper/global RAM behavior are outside this static contract; bounded copy mapping and coherent input/consumer binding remain mandatory. Item labels do not inflate `POI_TEXT`.

## Exact controls and evidence boundaries

The source-first assessment inspected eight public source files from pinned `pokegold` and `pokecrystal` revisions. Their English dictionaries were not used as Japanese semantic evidence. One exact pinned Japanese charmap was fetched from the existing Stage 371 citation (`scr-trees/pokegold_jpcrystalvc`, commit `f2b5db1deb0b8f2009d7e9d50b3bcb05ef8a9f53`). Existing dialect ratification was not reopened; disabled English expansions establish no Japanese consumer behavior.

Current traversal independently retained **225 Gold typed references / 62 unique IDs** and **263 Crystal references / 76 unique IDs**. Gold's count of 62 is coincidental, not inherited from Gen I. Gold contains **46 ordinary and 16 generated names**; Crystal contains **60 ordinary and 16 generated names**. Both domains include generated HM07 at item 249.

One bounded preimplementation compiled proof loaded each exact control once, completed one ROM0 nomination scan per control and found one candidate each. Eight direct windows retained **265 Gold / 263 Crystal code-and-directory bytes**. Each control executed 7,845 evaluator steps covering all **65 generated-byte cases**. Payload evaluation consumed **1,099 Gold / 1,255 Crystal packed bytes plus 11 prefix bytes each**, yielding independent name TSVs and **100 Gold / 108 Crystal consumed-token expectations**. No unsupported tokens, substitutions or disabled English expansions were consumed. Work stayed within the predeclared two-control budgets; no repeated scan, global-domain or live-display claim follows.

The bounded independent reviewer found no concrete blocker and replayed the retained compiled bytes and all generated cases, including banked return, saved carry, skipped-ID arithmetic and restoration. They cross-checked every name oracle, consumed-token declaration, embedded integration expectation and typed-reference inventory. **Raw name-payload and reference-operand bytes are not retained in this receipt:** their verification relies on the audited proof script, retained execution evidence and oracle consistency, not independent raw-byte replay. Nomination census likewise remains execution evidence. The review made no new ROM reads, network requests, builds or edits.

## Executed regression history

Retained actual XML establishes:

- Typed reference provenance: **3/3 expected failures** before instrumentation. An initial 21-test passing run was rejected for an incorrect extra selector; the corrected actual inventory passed **25/25**. No selector failure was waived.
- Separate original compiled authority: **3/3 expected failures**. The retained directory named `authority-green` still contains **two failures in three tests**, from incorrect code/directory accounting. It is not accepted green evidence. Subsequent combined and final gates cover the corrected authority.
- Session freezing, reference-scoped materialization, actual stale revision-56 SQLite rejection and both native item-name controls: **six expected failures in nine tests**, then **25/25 passing** after wiring. Token-oracle checks were not the failing assertions.
- Adversarial/identity follow-up: **11/11 passing**.
- First forced seven-control run: **252 tests, three failures, zero errors/skips**. All item-name/API checks passed; three GBA move-prose SQLite assertions retained the stale `56L` metadata literal. Those expectations were corrected to parser 57 / storage 2. This rejected run is not final acceptance.
- One-shot traversal cancellation: **one expected failure** because cancellation could become a skipped map. Both POI resolver layers now explicitly propagate cancellation rather than relying only on a second token check; focused corrected coverage passed **5/5**.

The stale-cache regression writes a synthetic catalog into **actual SQLite**, changes metadata to revision 56, asserts complete-cache rejection, and verifies revision-57 write/reopen. It is not a separate old-ROM reparse claim. The two actual revision-56 Gen II baseline catalogs were independently retained before producer wiring.

## Independent fresh acceptance

The coordinator verified the frozen handoff and patch hashes, all **15 source hashes**, their baseline blobs, **276 artifact hashes**, exact unstaged scope and actual staged red/green XML. Owner and coordinator separately passed the same forced **253-method gate: 196 parser + 50 store + seven exact native controls**, with `--rerun-tasks --no-parallel`. The fresh coordinator run completed in **6 minutes 36 seconds**, **45 tasks executed**, Gradle exit zero, no failures/errors/skips and all source hashes unchanged. Exact XML method inventories, freshness and executed task markers were checked. Existing Kotlin/Gradle warnings remain. Optional corpus methods were outside this focused gate, not counted as passes or skips.

Both new controls mandate every independent name and consumed token through parse/materialization, actual SQLite write/close/reopen and cache-only API startup. All API ball/POI item entries equal their reopened native names with **zero parser invocations**. Exact item-name keys equal the unique referenced capture-ball/POI domain. Fresh provenance matches the preimplementation traversal byte-for-byte.

Bounded read-only comparisons of owner and fresh databases check section inventory, contiguous chunks, compressed payload digests, decompression limits, integrity and foreign keys. All **17 shared sections per Gen II control**, unrelated overlay fields and the other localized capabilities equal revision-56 baselines; `POI_TEXT` is unchanged. The previous five controls retain all **18 catalog sections** unchanged: Red/Blue62, Yellow62, Ruby101, Emerald104 and FireRed127 item names.

The coordinator's first verifier preflight incorrectly treated the Gen II TSV header as an item and stopped before Gradle. Explicit header validation and unique-row/count checks corrected that verifier-only mismatch; the subsequent fresh gate passed without production changes or weakened expectations.

Private evidence retains the source snapshots, exact input locators, proof scripts, logs/XML, SQLite databases and receipts outside public assets. Fresh coordinator receipt SHA-256: `9592c20bdead10b99e51123d2d2215fa0da40c43f544032b6e9a7d5763c0d07b`. No ROM payload is published.

## Remaining Stage 4 work

Task 390 remains open for Korean ordinary/generated names and skipped-ID arithmetic, plus broader Western item support/acceptance. This exact two-control checkpoint does not ratify other inputs or a global item domain. Task 386 still owns remaining Gen II Local labels, GBA region-label authority/applicability and reference-aware POI coverage.

Tasks 373–375 still require all 15 capability dispositions across the current **43-cell/44-control** official matrix, including separately executed Korean Gold and Silver, one final current-corpus run after executable changes stabilize, ledger audit and published `stage-04-closure.md`. No required cell is waived. Stage 4 remains open; Stages 5–6 have not begun. No full corpus, device/emulator/ADB testing, signing, release APK or cleanup is part of this checkpoint.
