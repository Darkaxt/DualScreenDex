# Contextual Local-map authority checkpoint

Stage 4 remains **in progress**. This checkpoint addresses Tasks 414/416 and the contextual portion of Tasks 417/421 under `LNG-B002`/`LNG-B003`; it does not close the official matrix, title/sign obligations, or the localization program.

Source baseline: `30b3b145ec45e3e07b4129c82341db944698431b`, plus the source-pinned changes below. Synchronized `fork/master`: `2fef59df95f6deb0ae861aad42cff52eff739a86`; the implementation branch contains that baseline with no upstream-only commits. Source-bound gates ran before publication, against their exact recorded snapshots.

## Implemented contract

`LocalMapNameDisposition` separates `STATIC_NAME_REQUIRED` (the default) from `CONTEXT_DEPENDENT`. A missing ordinary name remains required and incomplete. Only a coherent compiled current-map → backup-map landmark selector, bound to the selected map directory and headers, permits a contextual classification.

The Gen II binder requires the current/backup operand pairs, common field consumer, selected bank/directory, nine-byte header stride, bank-switch register, return/control-flow shape, and zero landmark sentinel. Missing, malformed or competing proof grants no exemption. Cancellation remains propagated. Production routing uses compiled relationships, not filenames, hashes, language labels or fixed map-ID lists.

All numeric maps, keys, geometry, scenes and assets remain. The shared catalog exposes separate static-required and contextual key inventories. `LOCAL_MAP_NAMES` measures the static-required inventory; ordinary unnamed maps cannot disappear from that denominator. A contextual map cannot contain a fixed shared or overlay name. Projection and destination-reference joins cannot borrow its runtime backup label, another language, or nearby POI text. The separate POI expected denominator is unchanged.

Parser revision **63** invalidates revision-62 caches. SQL schema **2** and codec revisions **1** remain unchanged. Missing, null or unknown persisted dispositions reject the catalog; they cannot manufacture a contextual exclusion. Historical stale-revision fixtures remain stale, while current-write assertions verify actual stored metadata against the current schema constants.

## Retained verification

- Catalog/overlay/projection/cache isolated gate: **76 passed**, zero failures/errors/skips, following three genuine assertion failures on the old implementation. Mixed Japanese/Korean synthetic catalogs survive actual SQLite close/fresh reopen and cache-only projection. Seven persisted corruption variants fail closed.
- Compiled binder/Local/scenes/World isolated gate: **45 cases, 44 passed and one existing opt-in skip**, zero failures/errors. Thirteen binder methods exercise relocation, selected-bank/directory/header dependencies, competing and malformed candidates, bounds and cancellation. Two prior Local-entrypoint assertion failures establish the accounting regression.
- Revision-63 isolated gate: **30 passed**, zero failures/errors/skips. Its genuine prior RED showed both a fresh reader and fresh cache incorrectly accepting a revision-62 stamp. GREEN rejects that stamp, rewrites/reopens revision 63 and retains three maps: two static-required and one contextual.
- Independent review of the paired contextual source found no actionable verified defect. This is code review, not whole-input semantic acceptance.

The coordinator's first paired gate ran **180 cases: 173 passed, six failed and one existing opt-in skipped**, in 400.475 seconds, with unchanged source bytes. The six failures were a private runner selection error: native original-input reference tests were included and threw on the absent required input environment variable, before directory or ROM access. No original was read. Their failed XML and runner are preserved. The corrected private runner explicitly excludes those six methods alongside the six existing original-input cache methods. Its **174-case gate passed: 173 passed and one existing opt-in skipped**, zero failures/errors, in **89.821 seconds**. All three selected test tasks executed, and the coordinator verified exact XML inventory and unchanged source pins. Result SHA-256: `b817adcaf51edc92742fe496e7b74e805ab0b550dd3864a01ef9d11516a74b8e`. No production source or test assertion was changed to conceal a failure.

Sparse retained replay covers eighteen contextual identities (4/6/4/4 across Japanese Gold/Silver-family, Japanese Crystal, Korean Gold and Korean Silver). It uses 538 retained dependency bytes and **does not prove whole-original uniqueness**. Additive assertions for the four existing native round-trip selectors are integrated. The separately scoped whole-original run completed with four passing JUnit cases, but its wrapper failed on an output-format mismatch. Independent retained-only reconciliation now establishes **`INDEPENDENTLY_RECONCILED_SCOPED_ACCEPTANCE`** for those four contextual-map controls, without changing the original failed wrapper outcome or rereading originals.

## Separate Western Ruby continuation

The preceding [item/POI authority checkpoint](2026-09-08-item-poi-authority-checkpoint.md) remains an aggregate failure: twelve controls independently match **1,357 names**, while French/Italian/Spanish Ruby retain **303 unavailable required names**. All 1,660 requests remain mandatory; batch semantic acceptance remains false.

A new, separately reviewed bounded caller/copier capture passed 27 fabricated safety tests and retained-data preflight before one coordinator-scoped execution. It completed three original reads (50,331,648 bytes total) in 3.165 seconds, with no timeout or retry. Each control has eleven getter callers, 75 retained windows, 11,378 requested bytes and 8,308 unique retained bytes; direct-edge counts are 84/85/80. The coordinator reconciled complete inventories, bounds, overlapping bytes, evidence hashes and unchanged package pins. Receipt SHA-256: `451aa518829cc7540e3cddfababdd5524d661393857ffff95251cba4ca9c4f33`.

This reservation is consumed. The capture is diagnostic evidence, not name acceptance. Retained investigation establishes **zero existing copy contracts**, not multiple conflicting contracts: each control contains one complete 62-byte `r5`-destination wrapper whose ordinary getter is at offset +44. The generic correction binds that complete wrapper, its branches, register transfers, owned literal slot and complete ordinary copier. Only the unequal arm grants static-name authority; the exact compared ID remains excluded, and equal-arm payload/helper descendants stay opaque. Malformed nominated competitors remain terminal and unique-contract cardinality is unchanged.

The isolated behavioral RED ran seventy methods with four assertion failures; GREEN passed **70/70**, zero failures/errors/skips, in **47.585 seconds**. An initial Kotlin test-type-inference failure is retained separately, not counted as behavioral RED. The coordinator verified all handoff/source/evidence hashes and XML, reviewed the two-file diff, and integrated it with unpublished revision 63 after an independent review found no actionable defect. Handoff SHA-256: `39d772ff7038746727fbde86ea6ddc7502dfc7082af29d070c651335de7881bb`.

The first combined contextual/Ruby/materializer host gate ran **275 cases: 273 passed, one failed and one existing opt-in skipped**, in 119.368 seconds. The failure exposed an unchanged pre-checkpoint materializer fixture whose three POIs omitted explicit text obligations. Published reference-aware accounting correctly treats `UNRESOLVED` as incomplete, so its obsolete direct-text-only expectation was invalid. The corrected fixture declares two item-name references and one direct sign; it asserts two covered of three expected POIs, preserves all three identities and leaves dynamic item 175 unavailable. No production validation was weakened.

The corrected combined gate passed **274 tests plus one existing opt-in skip**, zero failures/errors, in **90.589 seconds**. All three test tasks executed; exact XML inventories and all 797 executable/configuration source pins were unchanged. Result SHA-256: `3b35ce5f01355aa9cfa725360cae2f1f95e9ffb14a182417318b149a94cf35af`.

The additive app assertions were recovered from a completed isolated **7/7** fabricated gate after the worker's context overflow interrupted its handoff. Earlier five-case RED had four assertion failures; a subsequent six-case receipt RED had one. The isolated gate used parser revision 62 and is not combined-revision-63 evidence. After integration, the first parent app attempt failed before Gradle because its private source inventory incorrectly rejected a published MIT CPU conformance fixture; it read no original. The standalone parent-revision-63 gate then passed **7/7**, zero failures/errors/skips, in **314.456 seconds**. The app test task executed, and the coordinator verified exact XML inventory and all 797 unchanged source pins. Result SHA-256: `c95093942d992b274c80088d0cb3fdcd8365236aedc7fe77e7f6792a318cd8d8`.

The native acceptance runner's durable receipt correction and exact path/hash-bound MIT fixture inclusion passed **24 safety cases** and independent delta review. Its final freeze binds 916 source/configuration inputs, four exact controls, one read/parse per control, zero API reparses and a 1,200-second owned-process watchdog. Freeze SHA-256: `baf10904645e7c2babd02437f08c5570a5161b705901d750968d7f9d88d1f4b3`. The separately scoped one-shot run completed in **341.076 seconds**, with Gradle exit **0**, no timeout and **four passing JUnit cases**, zero failures/errors/skips. Its wrapper then failed because it expected numeric `NativeChecks` counts, while the existing app emits comma-separated check-name lists. The raw XML preserves all four result lines and contextual receipts; the failed wrapper outcome and consumed reservation remain unchanged. The separate retained-only reconciler passed **9 fabricated cases**, following two genuine RED failures against the old grammar. Its final 15-artifact package was independently hash-verified. The coordinator independently checked exact XML/receipt identities, all per-key numeric geometry and names across producer → fresh SQLite → cache-only API, and actual read-only SQLite metadata **63/2** in four distinct databases. All **1,492 maps** remain: **1,474 static-required names are covered**, and **18 positively contextual maps carry no fixed name**. The captured run has four original parser invocations and zero API reparses; reconciliation performed zero new original accesses, parses or Gradle executions. The frozen and recorded-at-capture 916-source inventories agree. Reconciliation SHA-256: `ee2a1c42e6fd1333e2393fc35c2796d4031d985867bd03d2e7e34500bad6eb83`. Original failed outcome SHA-256 remains `3b2fb699814e541db5a7795a978fcdf94f6967b7c2662a897bea3d62f5c7b7b4`. This independently accepted scope is contextual-map disposition only, not Japanese Crystal direct-sign or overall native/Stage 4 closure.

The fresh Western runner's **23-case** receipt-durability correction and independent delta review passed. A subsequent three-file private build/environment-isolation correction passed **12 relevant safety cases**; the coordinator reviewed that delta and verified all 85 handoff pins. Its final **931-source** freeze, SHA-256 `b0111916db486c1d1f275d920e8ed65a6a0f5c02a05cfa47e515aeab57f45b6a`, binds the independent fixture, original selected routes, 37 methods, all 1,660 requests and 4,470 API occurrences. A separate coordinator-scoped decision preceded the actual fifteen-control gate launch, with one read/analysis/producer per control, zero API reparses and a 2,400-second watchdog. The run completed in **1,067.726 seconds**, exit **0**, no timeout, with **37 passing JUnit methods**, zero failures/errors/skips. Its result reports all **1,660 names** accepted across fifteen controls and **4,470 API occurrences**, with fifteen reads, fifteen original analyses, fifteen item producers and zero API reparses. The coordinator independently verified all 931 live/frozen source hashes, 332 retained-output pins, four XML pins, exact method/control inventories and complete six-boundary receipt counters. Result SHA-256: `4bdcba7fa2d8bedaab075ad6f9e7ff093960892738df05e7bcb35d3e58e1a571`. Independent retained semantic verification also passed: all fifteen controls match the pinned fixture at all six boundaries, including every API occurrence, exact selected routes and requested domains, the dynamic-175 exclusion and mandatory FireRed ID zero. No whitespace normalization was needed. Projection comparisons were reconstructed from captured fields and read-only SQLite sections; separate direct projection-result files do not exist. Recorded execution counters were cross-checked, not represented as new execution-time instrumentation. Audit SHA-256: `11e6bdb5520e77cc33148ade881f27fc81c6abd34c7636d61b77b35f6721da0f`. This closes the scoped 1,660-name acceptance requirement; the historical failed gate remains immutable. Historical consumed gates are never reused.

## New bounded declaration captures

The Japanese declaration core passed its retained/fabricated gate; its separate loader's receipt-initialization durability defect was reproduced with five failing cases and corrected, with **28/28** runner cases passing. After independent correction review and coordinator hash reconciliation, one separately scoped capture read **2,097,152 bytes once**, retained **708 new bytes** alongside the existing 532-byte union, and exited successfully with `DECLARATIONS_CAPTURED_NOT_PROOF`. It retained three nominations and used 1,287 bounded work checkpoints; metadata and package/source pins stayed unchanged. The worker was reaped and its buffer released. Receipt SHA-256: `a849a3015fe64d4c6037a0414346231d6993d069a51867af0d7a3d0d21c2bd41`. This is not production sign or glyph-semantic acceptance.

The corrected GBA package preserves rejected/competing candidates and separate open/read attempt/completion accounting. Its **59-case** fabricated/retained gate and correction review passed before a separately scoped three-input capture. That capture completed **three reads, 41,943,040 bytes**, in **66.480 seconds**, with unchanged package pins and no timeout. All three diagnostic results were `PARTIAL_STOP_NO_EXPANSION`, so the collector correctly exited **2**, not success. It retained 184/186/174 windows and 18,906/18,084/23,184 new bytes, with zero reused capture bytes. Receipt SHA-256: `16a6b131b3923edd110b0070782ab6777a76b230db20a73a9d28f3ed8d8272b2`. Rejected-candidate evidence is retained for analysis; partial/no-match results do not prove absence or justify title exclusions.

Retained Japanese analysis identifies three concrete production-shape gaps: template opcode roles, an inline textbox envelope, and an ordinary-scalar dictionary fallback distinct from the Korean lead-byte grammar. The isolated correction passed 34 synthetic cases plus one guarded retained-pattern diagnostic. Independent review then reproduced a generic fail-closed defect: discarding the compiled END opcode lets a relocated glyph-shaped END be decoded as content. That narrow correction remains in progress, outside this parser-63 checkpoint. The existing selected map/event structural root remains required; pattern replay and missing nonselected dictionary bodies do not establish whole-root or global role uniqueness.

Retained GBA analysis confirms that the complete indexes were built and all planned windows captured. Unrelated battle/chat graphics loaders exhausted candidate inventories, while neighborhood literals exhausted the pointed-operand budget before coherent map relationships were retained. These are concrete negative regression cases, not positive region-title authority. A separate private diagnostic correction passed 70 synthetic/retained cases against those negatives and source-shaped positives under unchanged bounds, and its focused delta review found no further admission/allocation blocker. A separately durable coordinator launcher is being prepared around the frozen collector. No further acquisition is authorized by that implementation work.

Both new reservations are consumed. There was no retry, automatic expansion or full-original retention. Raw declarations and exact paths remain private. Production sign/title authority and independent semantic acceptance are still required.

## Required continuation

| Ledger ID | Owner / target | Acceptance still required |
|---|---|---|
| `LNG-B002`, `LNG-B003` | Tasks 417/421 — contextual Local names | Current-source host and four-control acceptance verified, including exact static/contextual partition, all maps retained, complete ordinary names, no fixed contextual labels and zero API reparses. This checkpoint delivers that scoped correction and acceptance; final stable-source matrix ratification remains separate. |
| `LNG-B002` | Tasks 414/416 — Western GBA names | Scoped current-source acceptance met: all 1,660 independently exact names at six boundaries, including 4,470 API occurrences. This checkpoint delivers the correction/evidence while preserving the failed historical gate; final stable-source matrix remains separate. |
| `LNG-B002`, `LNG-B003` | Task 419 — GBA region-title authority | Analyze the consumed, bounded three-input partial capture and preserved candidate inventories; prove compiled text authority or independently justified applicability. Partial/no-match is not absence and does not permit an automatic reread. |
| `LNG-B002`, `LNG-B003` | Task 420 — Japanese Crystal sign grammar | Analyze the consumed one-input declaration capture, then bind the observed script's compiled opcode and payload through generic production/reopen/API behavior; diagnostic declarations or source opcode labels alone are insufficient. |
| `LNG-B001`–`LNG-B003`, `LNG-D005` | Tasks 373–375 — Stage 4 closure | Current 43-cell/44-control matrix, all fifteen capability dispositions and remaining sign/service/title obligations, final forecast-policy ratification, one final current-corpus run after executable changes stabilize, audited ledger and published closure. |

Stages 5–6 remain blocked on sequential published closure. No full corpus, Android/device/emulator testing, APK, signing or release execution occurred for this checkpoint. Raw payloads, exact input paths and private fixtures remain outside public assets.
