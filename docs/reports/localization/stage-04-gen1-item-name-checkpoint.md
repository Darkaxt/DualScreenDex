# Stage 4 Gen I item-name checkpoint

Status: **independently verified: 213/213 tests passed, zero failures/errors/skips**. This completes the exact Japanese Red/Blue-family and Yellow referenced item-name slice of Task 390 / Task 386 / `LNG-B002`, not Task 390 or Stage 4 as a whole. Parser revision is **56**, storage schema remains **2**.

## Static reference-scoped authority

Both revision-55 controls retained **62 unique referenced item IDs** but no localized item names. Valid native codecs and numeric POIs did not themselves supply item-name consumer authority.

The implementation establishes this narrow contract: publish a static label only for an independently authorized item reference, using the uniquely selected compiled consumer and bounded evaluation of that input. The original identity phase freezes immutable compiled naming mechanics in the analysis session. Current accepted visible/hidden item extraction retains typed operand/map/handler provenance; historical cache ID membership alone is not that certificate. The materializer evaluates only those references against the frozen contract, without consumer rediscovery or later root selection.

The compiled ordinary path selects a bank and directory entry, walks one-based 0x50-delimited records, and copies **20 bytes** from the selected start. Each requested start and terminator must be established within shared traversal limits, and the entire copy span must fit the static mapped ROM window. Exact-codec decoding must terminate within the copied output with no invalid, control or substitution tokens. The mapped bank end is a safety ceiling, not a semantic whole-table boundary; traversing unrequested rows grants them no authority.

Generated names use separately bound compiled formatter arithmetic and prefixes: **six bytes for HM, five for TM**, followed by two digits and a terminator. Arithmetic maps 196–200 to 1–5 and 201–255 to 1–55, but this does **not** establish global valid-item semantics or authorize TM51–TM55. Unrequested inputs and zero remain unavailable. No source TM50 bound or global item count is imported. Static own-buffer evaluation preserves the proven store order/nonalias conditions without claiming arbitrary live RAM or graphics behavior.

Conflicting/incomplete consumers, malformed branches, mapped-span failures, bounded-work exhaustion and cancellation fail closed. Original GBA terminal authority and numeric selection remain unchanged. All referenced IDs remain in the coverage denominator; localized labels do not inflate `POI_TEXT`.

## Evidence boundaries and exact controls

The Red/Blue-family control is Japanese Midori Rev 1; the Yellow control is Japanese Pikachu Rev 3. Each independently reconstructed domain contains **35 ordinary names and 27 generated TM names**, with no HM, zero or floor-name references. These controls therefore do not ratify a complete HM/floor/global item domain; generated HM mechanics are covered by bounded compiled proof and synthetic tests.

The source assessment and retained compiled-mechanics proof preceded production implementation. An initial demand for a global valid-item count and root-bound whole-table extent was independently reviewed and superseded by the approved per-reference contract. Live mapper-state and global RAM-allocation proof were likewise stronger than static label interpretation requires. These corrections do not relax typed input provenance, mapped copy bounds or exact-token requirements.

Current-path provenance retained **150 Red/Blue and 161 Yellow typed references**. A bounded payload proof reconstructed all 62 labels per control using **641 shared packed bytes plus 11 prefix bytes**, before producer/schema wiring. Six rejected queries consumed no payload. Only two exact native charmaps were fetched from the Stage 371 citations: `pokered-jp` commit prefix `258d1a89` and `pokeyellow-jp` commit prefix `f282e72a`; full source pins and content hashes remain in the private proof receipt. The existing ratified dialects use ASCII digits for Red/Blue and fullwidth digits for Yellow. Shared-tile aliases are represented by their canonical Hiragana mapping, not inferred item spelling.

The independent reviewer replayed the retained payloads and all **89 consumed token mappings per control**, reconciling the charmaps, pinned-source records, TSV oracles and embedded integration expectations. This review made no fresh ROM reads or network checks. Raw reference-operand assertions rely on retained executed evidence plus producer review; payload decoding was independently replayed. No whole-program, live-display or whole-table authority is claimed.

## Executed regressions and independent verification

Retained actual XML establishes:

- Typed provenance: one expected failure in two tests, then **4/4 passing**, with the original assertion preserved.
- Relocated compiled authority and budget/cancellation: two expected failures, then **4/4 passing**.
- Missing ordinary/generated labels, original-phase freezing and actual stale revision-55 SQLite rejection: **four expected failures in 25 tests**, then **29/29 passing** after wiring.
- A competing nomination at the final possible ROM0 offset: one expected failure while both native controls passed. The inclusive nomination boundary was corrected and retained in the final gate.

The stale-cache regression uses a synthetic catalog in **actual SQLite**, not a claim that an old-ROM run itself tests invalidation. Revision-55 complete caches are rejected; revision-56 write/reopen succeeds. Separate exact-control revision-55 Gen I baselines were retained before producer wiring.

The coordinator verified the frozen handoff, patch hash, all **15 source hashes**, **174 artifact hashes**, exact working-tree scope and actual red/green XML. A bounded independent code review found no concrete blocker in the supported static contract. The owner and coordinator separately ran the same forced **213-method** gate: **159 parser + 49 store + five exact native controls**, with `--rerun-tasks --no-parallel`, zero failures/errors/skips. The coordinator run completed in **5 minutes 57 seconds**, **45 tasks executed**, Gradle exit zero and all source hashes unchanged. Fresh XML method inventories and executed task markers were checked. Existing Kotlin/Gradle warnings remain; optional corpus methods were outside this focused gate, not counted as passes or skips.

Both Gen I controls mandate all **62 independent oracle names**, exact referenced-ID equality, all 89 consumed token mappings and the dialect-specific digit distinction through parse/materialization, actual SQLite write/close/reopen and cache-only API startup. Every API item entry equals its reopened native name with **zero parser invocations**. Fresh structural provenance is byte-identical to the preimplementation traversal.

The coordinator independently reopened owner and fresh databases read-only with bounded section/chunk/decompression checks, payload digests, integrity and foreign-key checks. All **17 shared sections per Gen I control**, unrelated overlay fields and localized capabilities match revision-55 baselines. `POI_TEXT` is unchanged. Japanese Ruby **101**, Emerald **104** and FireRed **127** names remain available, and all **18 catalog sections** for each GBA control are unchanged from their accepted revision-55 outputs. Ruby additionally matches every independent oracle name.

A private comparator initially assumed the old overlay stored null-valued item-name entries. The existing extractor actually stores an empty map and retains the **0/62** capability denominator and numeric POIs. The comparator was corrected against those records; this was not a production failure or coverage waiver.

Private input paths, raw windows, decoded oracle records, source snapshots, logs/XML, SQLite databases and full receipts remain outside public assets. No ROM payload is published.

## Remaining Stage 4 work

Task 390 remains open for Japanese Gen II ordinary/generated names, Korean skipped-ID arithmetic and broader Western support/acceptance. This exact two-control checkpoint does not ratify unreferenced Gen I item classes. Task 386 still owns remaining Gen II Local labels, GBA region-label authority/applicability and reference-aware POI coverage.

Tasks 373–375 still require all 15 capability dispositions across the current **43-cell/44-control** official matrix, one final current-corpus run after executable changes stabilize, ledger audit and published `stage-04-closure.md`. No required cell is waived. Stage 4 remains open; Stages 5–6 have not begun. No full corpus, device/emulator/ADB testing, signing, release APK or cleanup is part of this checkpoint.
