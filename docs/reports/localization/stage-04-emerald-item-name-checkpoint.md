# Stage 4 Emerald item-name checkpoint

Status: **independently verified: 242 passed, six existing optional-control skips, zero failures/errors**. This completes the Japanese Emerald ordinary item-name slice of Task 390 / Task 386 / `LNG-B002`, not Task 390 or Stage 4 as a whole.

## Producer and authority

Item names previously had no producer for the referenced ball/POI item domain. Existing language overlays, persistence and API projections already supported them. A session-owned materializer now resolves the unique final referenced IDs and joins names into existing ball/POI fields before localized-text extraction. Shared numeric records remain language-neutral, and secondary projections cannot borrow names from the default language.

The original published-header selection supplies a nomination only. Its item root and absent/ambiguous state are retained through identity resolution and final layout assembly; later table counts cannot re-select a different block or break an earlier ambiguous tie. Existing block ranking is unchanged. Compiled consumer proof, not a published slot, title, hash, filename or language identity, establishes name authority.

The exact Japanese Emerald witness proves:

- A complete u16 sanitizer and row-pointer getter: **377 records, 40-byte stride**.
- A separately bound scalar getter establishing the **10-byte inline name boundary**.
- An ordinary copy-wrapper branch and complete `0xFF`-terminated copier, with constrained direct effects and balanced returns.
- A compiled dynamic-name exception at input **175**. Its readable table row is not an ordinary-name authority and remains unavailable when referenced; it is not removed from the expected domain.
- A legitimate row zero. Out-of-domain IDs are withheld rather than advertised under the game's row-zero fallback.

All 15 nominated root literals and 14 LDR users were reconciled before decoding. The bounded proof used two 16-MiB nomination passes, 14,792 backward-reference bytes and 1,580 semantic bytes, including an explicitly repeated 320-byte sample read after a console-encoding failure prevented the initial receipt from being saved. This was exact-control research, not a production identity profile. Public source and an independently pinned charmap supplied structural/sample oracles only.

Production reuses the shared reference index, charges bounded recovery and caller nomination, memoizes terminal outcomes and propagates cancellation. Conflicts, incomplete evidence, exhausted budgets and selected unavailable outcomes cannot retry an unproven fallback. Names require exact projection tokens, no invalid or unresolved control/substitution units, and termination within the independently bounded field rather than numeric bytes or the next row. The contract is static ordinary ROM-authored names, not dynamic berry state, pluralization or arbitrary live rendering.

## Tests and evidence integrity

Red tests established the missing producer, stale revision-53 cache acceptance, missing catalog join and retained-nomination integration requirements. Positive-derived relocated tests cover instruction/dependency changes, independently varied geometry, conflicting witnesses, incomplete references, budgets/cancellation, truncated extent/caller overflow, row zero/last row/out-of-domain IDs, the dynamic exception, token boundaries and projection isolation.

Parser revision is **54**, storage schema remains **2**. Six existing current-version assertions initially still expected 53 and failed; only those expectations were corrected, preserving stale-version SQL mutations. That interrupted run executed the store suite only. Accidentally copied older parser/app XML was explicitly excluded from its receipts, not counted as current execution.

The final owner gate ran 123 cases: **117 passed, six existing optional-control skips, zero failures/errors**. Its command did not use `--rerun-tasks`; executed task markers, fresh XML, required suite/method inventory and unchanged source hashes were checked. The coordinator verified all eight retained XML hashes and the complete patch hash. A bounded independent read-only review of all 16 source/test files found no concrete correctness defects.

The coordinator then ran a fresh combined gate with `--rerun-tasks --no-parallel`: **248 cases, 242 passed, six existing optional-control skips, zero failures/errors**, in **9 minutes 44 seconds**, with **45 tasks executed**. All 16 source hashes remained unchanged. Coverage includes item/header/catalog/overlay/ball tests, native move prose and species-index regressions, Gen II sign/cancellation tests, the full store suite, five exact native controls and 12 forecast cases. No selected native control was skipped.

The six skips are existing store opt-ins for Unbound, Modern Emerald, official Gen III trainer sprites, Odyssey, official Gen II Local-map assets and official Gen I Local-map assets. They are not represented as executed controls. Existing compiler and Gradle deprecation warnings remain.

## Exact-control outcome

Japanese Emerald reports **ITEM_NAMES AVAILABLE 104/104** through materialization, actual SQLite write/close/read/close and API projection. Five independently pinned ordinary ball/item/key/machine digests pass at each boundary. Cache-only bootstrap retains `ROM_DEFAULT` and **zero reparses**. Separate real-row assertions cover zero and the final row, while dynamic 175 and out-of-domain 377/65535 remain unavailable.

The coordinator separately reopened both owner and fresh-run Emerald databases read-only. All **104 names** exactly matched the independent charmap oracle. All **17 non-overlay sections**, all other overlay domains and all other localized capability states matched the revision-53 baseline. `POI_TEXT` was not inflated by item-name coverage. Integrity checks passed with zero foreign-key violations.

The same fresh gate preserves Ruby/Emerald/FireRed move prose at 354/354, native GBA species-description applicability at 386/386, and both Korean declared-sign controls. It also observes **FireRed ITEM_NAMES AVAILABLE 127/127** from the generic resolver, without another ABI route. This is an observed output gain, **not independently sampled FireRed item-name ratification**; the dedicated FireRed item gate, including its referenced zero, remains required. Ruby remains 0/101 and Korean Gold/Silver each 0/62 for item names.

Private exact-control inputs, proof receipts, source snapshots, commands, logs/XML, independent oracles and SQLite databases remain outside public assets. No ROM payloads are published.

## Remaining Stage 4 work

Task 390 / `LNG-B002` still owns Ruby item authority, independent FireRed acceptance including referenced zero, Japanese Gen I/II packed names and generated TM/HM names, Korean skipped-ID machine arithmetic, and broader Western item ABI coverage. These are required follow-ups, not applicability waivers. The current checkpoint adds synthetic Western-compatible coverage but does not reratify the 35-control Western matrix.

Task 386 still owns remaining Local/region-label authority/applicability and reference-aware POI coverage. Tasks 373–375 still require all 15 capability dispositions across the current 43-cell/44-control matrix, the final current-corpus run after executable changes stabilize, ledger audit and published `stage-04-closure.md`. Stage 4 remains open and Stage 5 blocked. No corpus, devices/emulators/ADB, signing, release APK or cleanup is part of this checkpoint.
