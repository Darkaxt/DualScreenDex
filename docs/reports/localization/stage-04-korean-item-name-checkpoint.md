# Stage 4 Korean item-name checkpoint

Status: **independently verified: 264/264 tests passed, zero failures/errors/skips**. This completes the separately executed official Korean Gold and Silver referenced item-name acceptance slice of Task 390 / Task 386 / `LNG-B002`, not Task 390 or Stage 4 as a whole. Parser revision is **58**; storage schema remains **2**.

## Static reference-scoped authority

The supported contract remains: publish a static label only for an independently authorized item reference, using the uniquely selected compiled consumer and bounded evaluation of that input. Original-session immutable authority and current typed visible/hidden references remain mandatory; materialization does not rediscover or reselect a consumer. Exact item-name keys equal the unique referenced capture-ball/POI item domain, not a guessed whole-table domain. Missing names remain in that denominator, and item labels do not inflate `POI_TEXT`.

The source-first assessment used the clean pinned public `Narishma-gb/pokegold-kr` checkout at `7743877dc9fa8603f4b6eaebe904a7ba03fdb9e4`. The compiled Korean consumer fits the existing Gen II resolver shapes, but its own constants are independently established: a **21-byte copy**, not the source item-name length of 17; **eight-byte literal prefixes** for 기술머신 and 비전머신, not a substitution token; ASCII digits; TM-before-HM classification, skipped IDs C3/DC, split F3 and HM subtraction 50. The banked helper, C result, saved carry, owned scratch/stack/store order and return relationship are bound.

The compiled `GetNthString` deliberately walks raw byte `50`; the renderer separately consumes multibyte Korean tokens. The materializer preserves that compiled indexing instead of replacing it with token-aware table traversal. Each requested start and full copy span must fit the bounded static bank mapping. Token validity, copy bounds, coherent input binding, current provenance, conflict/budget/cancellation outcomes and local malformed-name failure remain independent requirements. Global item counts, whole-table semantic extent and live mapper/global RAM behavior are not claimed.

## Exact-control proof and limits

Both controls independently retain **225 typed references / 62 unique IDs: 46 ordinary and 16 generated names**, including referenced HM07 at item 249. Parser 57 already produced these 62 names in each control. This checkpoint establishes mandatory independent Korean acceptance and corrects a synthetic malformed-prefix defect; it does **not** claim that official Korean names changed.

Exactly one bounded preimplementation proof process loaded each two-MiB control once and completed one ROM0 nomination scan per input, finding one candidate each. Per control it retained **265 compiled code/directory bytes in eight direct windows**, evaluated **16,167 steps**, consumed **1,478 shared packed bytes plus 16 prefix bytes**, and produced **62 independent names / 129 consumed-token expectations**. Five raw witness roles per typed reference provide **1,125 witnesses per control**. The process completed in approximately 0.222 seconds, within its predeclared two-control limits; no proof rerun or automatic budget extension occurred.

The independent proof reviewer replayed retained compiled bytes, every name/token and all reference witnesses against the pinned source algorithms and raw evidence. Ordinary instruction-level payload replay covers **IDs 2 and 152 only**; all 46 ordinary names are independently checked through the shared compiled bytewise walk, full-copy bounds and token decoding. All 16 generated names receive instruction-level payload replay. Nomination completeness and full event-directory membership remain retained execution evidence. This is not a full-dialect, global item-domain or live-engine validation claim. The production codec was not used as its own oracle.

## Regression and correction history

- Initial parser-57 native baseline: **2/2 passing**. Strengthened current-provenance capture: **4/4 passing**, after a nullable test-ID compilation correction. Both controls independently establish the 225/62 domains; Japanese counts are not assumed.
- Six relocated Korean materializer regressions cover 21-byte copy/prefix behavior, current/original reference authority, full-bank copy bounds, malformed ordinary tokens, raw-byte indexing through a terminator-like trail, and invalid/split literal prefixes.
- The first focused acceptance attempt ran **36 tests / three failures** because negative fixtures mutated source bytes after `RomImage` had copied them into a session. Creating sessions after mutation corrected that fixture error without weakening assertions.
- The corrected focused attempt ran **36 tests / one genuine production failure**. The synthetic eight-byte prefix ending in a Korean lead borrowed the appended generated digit as its trail and incorrectly published **기술머꼿1**. The actual XML, not an earlier mistyped glyph or history-string encoding, is the failure authority.
- An actual synthetic catalog written into **SQLite** and marked parser 57 was incorrectly accepted by the revision-57 reader: **one test / one expected assertion failure**. This is a real stale-cache rejection test, not an old-ROM reparse claim.
- The minimal correction validates each Gen II literal against a **prefix-only `RomImage`** using the exact codec before appending generated digits. Only complete glyph/whitespace tokens are accepted. Invalid, split, control, substitution and terminator tokens fail closed. Null memoization, cancellation, raw-`50` ordinary traversal, reference denominators and Gen I/GBA behavior are preserved. Parser **58** invalidates potentially false revision-57 persisted names; storage stays **2**.
- Focused corrected acceptance passed **37/37**. The owner then passed **264/264: 204 parser, 51 store and nine native controls**, with zero failures/errors/skips, in 7 minutes 14 seconds, 45 tasks executed. The previously planned 263-test final gate never ran; a dormant runner argument-slicing error was corrected before invocation, and the stale57 regression accounts for the final additional method.

The bounded corrective review found no concrete blockers in the production fix, session-after-mutation negatives, cache invalidation or acceptance literals. Every embedded Korean name/token expectation matches the already independently verified oracle. Existing signs, descriptions, content, other native controls and forecast checks are retained. The review performed no new builds, ROM access, proof research or cache execution.

## Independent fresh acceptance

The coordinator verified the frozen handoff/patch, all **seven source hashes and baseline blobs**, exact unstaged scope, **229 artifact hashes plus the patch**, retained genuine REDs and actual focused/final XML inventories. One auxiliary focused-evidence check initially looked for a nonexistent handoff field; it was corrected to the retained summary/XML schema and verified **37/37**, without production changes or test reruns. The earlier retained-cache precheck also corrected a two-column assumption to the actual strict four-column Korean oracle format before acceptance.

A separate fresh coordinator gate passed **264/264: 204 parser + 51 store + nine exact native controls**, with `--rerun-tasks --continue --no-parallel`, in **7 minutes 32 seconds**, **45 tasks executed**, Gradle exit zero and zero failures/errors/skips. Exact XML methods, freshness, executed task markers and all seven unchanged source hashes were checked. Existing Kotlin/Gradle warnings remain. Optional corpus methods were outside the focused selection, not counted as passes or skips.

Both Korean controls require all **62 independent names and 129 token expectations** through parse/materialization, actual SQLite write/close/reopen and cache-only API startup. All **225 item-reference API records** preserve numeric identity, location, kind and collection flags; every ball/POI item label equals the reopened native name with **zero parser invocations**. Both declared-sign samples per control remain independently asserted. Fresh provenance, raw witnesses and production-coverage captures are byte-identical to the preimplementation evidence.

Bounded read-only owner and fresh cache checks validate integrity, foreign keys, exactly 18 sections, contiguous chunks, compressed digests and decompression limits. **All 18 content sections are unchanged from parser-57 baselines for all nine controls**: Japanese Gold/Silver62, Crystal76, Red/Blue62, Yellow62, Ruby101, Emerald104, FireRed127, Korean Gold62 and Silver62. Metadata also agrees except parser revision and write timestamp. Native counts, all capabilities, earlier API assertion boundaries and official forecast-policy evidence remain intact; `POI_TEXT` is unchanged. Japanese GB/GBC typed-reference captures remain identical to their accepted baseline.

Private retained evidence includes source snapshots, exact-control locators, bounded proof/raw witnesses, independent oracles, logs/XML, SQLite databases and receipts. No ROM payload or private path is published. Fresh coordinator receipt SHA-256: `21b7341bc15a2ef7b2c3373d71d5c11539301b279b2dfa716f1d11ccd7433a3f`.

## Remaining Stage 4 work

Task 390 remains open for broader Western item support/acceptance. Task 386 still owns the four Gold/Silver and six Crystal Local-label gaps, GBA region-label authority/applicability and reference-aware POI coverage. This checkpoint does not waive any required field or ratify other inputs.

Tasks 373–375 still require all 15 capability dispositions across the current **43-cell/44-control** official matrix, one final current-corpus run after executable changes stabilize, ledger audit and published `stage-04-closure.md`. Stage 4 remains open; Stages 5–6 have not begun. No full corpus, device/emulator/ADB testing, signing, release APK or cleanup is part of this checkpoint.
