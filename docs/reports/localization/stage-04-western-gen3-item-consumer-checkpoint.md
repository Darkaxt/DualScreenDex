# Stage 4 Western GBA compiled-consumer checkpoint

Status: **retained diagnostic analysis and synthetic recognizer checkpoint verified; all 1,660 required names remain unaccepted**. This is a bounded Task 390 / `LNG-B002` checkpoint, not official-control or Stage 4 closure. It follows the immutable [Western GBA diagnostic](stage-04-western-gen3-item-name-diagnostic.md).

## Retained evidence and actual execution

The exact scope remains fifteen controls: English, French, German, Italian and Spanish, each with Ruby/Sapphire, Emerald and FireRed/LeafGreen. Requested names remain 101, 104 and 127 per language respectively. Original Ruby routes are `NotInvoked`; the other ten are `InvokedNominated` and must bind their original selected nomination. Incidental routes cannot replace them.

Task 401's first bounded capture remains an immutable unproved result. Task 404's retained-only geometry correction established ten nominated MUL-based getter inventories without granting name authority. Task 405 then prepared a separate supplemental collector with records collected before optional wrapper analysis, explicit incomplete outcomes, and immutable per-input reservations. Its fabricated gate passed **43/43** once after one expected RED failure; parent review reconciled the final frozen code and predecessor pins without rerunning the gate.

A separate coordinator decision preceded Task 406's **one** supplemental capture: exit **0**, **102.939 seconds**, fifteen input opens and fifteen logical reads, no timeout or retry. Independent retained reconciliation passed on its first execution:

- Ten nominated controls retain **1,155 raw diagnostic records / 50,820 bytes**.
- Five Ruby controls retain no records; all **505 requested IDs** explicitly remain unproved.
- **1,313 new windows / 902,291 retained output bytes** remain private.
- All **45 predecessor evidence/receipt/reservation pins** are unchanged.
- Every control terminates `TERMINAL_PARTIAL_UNPROVED`; accepted names remain **zero**.

Both acquisition reservation sets are consumed. Neither runner may be retried or redirected to bypass its reservations. Another original read requires a separately reviewed scope and coordinator decision; none follows from this report.

## Verified consumer distinctions

Task 407's independent review reconciled **143 unique artifact pins and 1,498 retained-window occurrences**, then the parent reviewed its complete verification source and pinned result. No owner transformer, historical checker, successful test gate or acquisition was rerun.

| Evidence | Verified conclusion | Boundary still open |
|---|---|---|
| Five Ruby getter inventories | Each independently has stride 44, count 349, one pointer getter and thirteen scalar fields. A competing broad-prefix hit returns a normalized unsigned value, not an indexed pointer. | A further root consumer uses an `r1` item input and field-20 load but lacks **26 interior bytes per control**. Full consumer/name authority, caller evidence and all 505 record payloads remain absent. |
| Nine ordinary wrappers | Each complete **26-byte** wrapper passes the indexed getter result directly to a copier; the getter call is at +8 and copier call at +16. | The nine canonical copier prefixes are absent from retained windows. Their suggested 30-byte probe sizes are not asserted function extents. |
| English FireRed wrapper | The complete ordinary branch and FF copier are retained. All **127 requested IDs**, including zero, take the ordinary branch. | ID 175 takes a distinct branch. Its absence from requested IDs does not prove its dynamic semantics. |
| Nine alternate copy-and-pad helpers | Complete **80-byte** helper bodies plus their own four-byte literals copy every byte before FF, then pad to a minimum width. Width 8 does **not** truncate longer content. | Local caller fragments require a nonzero ID: **1,024/1,028 nonzero-domain eligibility**, not caller or name acceptance. Four localized FireRed ID-zero requests branch away. Enclosing entry/return, reachability, external branch bodies and destination capacity are unproved. |
| Spanish FireRed literal overlaps | Five words have bounded local literal ownership. The pinned complete aligned direct-encoding scan reports 23 getter-call encodings and zero pool-target hits. | Encodings are not automatically executable instructions. Indirect/ARM/unvalidated execution remains outside the result; `globalNoIncoming=false`. |

A complete helper does not establish its caller's reachability, source termination or destination capacity. Conversely, the absence of one canonical helper is not an automatic requirement to acquire it if another independently complete consumer contract can establish the needed static relationship. The current alternate caller fragments do not yet do so.

The owner's two diagnostic correction explanations match the retained final observations: helper offsets were corrected, and four ID-zero exceptions replaced an invalid all-nonzero assumption. Corrected facts were independently byte-checked. Original failure chronology and packaging failure remain recorded-only, not independently reconstructed execution evidence.

## Existing token policy is sufficient for observed spans

Task 408 and its parent directly reconciled all **1,155 untyped leading 14-byte spans** against retained records and the existing exact Western codec version 1:

- One FF terminator per span; every content span is nonblank.
- **9,409 ordinary glyph bytes**, **497 content spaces**, **1,155 terminators**, and **5,109 zero-padding bytes**.
- Zero invalid, control or substitution tokens. Bytes `53`, `54`, `FC` and `FD` are absent even in padding.
- Ruby's 505 requests are unobserved, not successful zero-token cases.

No codec exception, alphabet-wide renderer gate or relaxed materializer policy is needed for this observed set. These remain untyped byte spans until consumer authority is established; termination alone does not make them accepted item names.

## Independent reference discovery

A bounded public-source search located [PKHeX's Gen III display resources](https://github.com/kwsch/PKHeX/tree/e0e63bc87837ad2d9c8f8fda4efdbf5f2933db08/PKHeX.Core/Resources/text/items/gen3), pinned to commit `e0e63bc87837ad2d9c8f8fda4efdbf5f2933db08`. Five language lists and their source consumers/license were retained privately. All requested IDs have rows, but that is **not exact-name acceptance**: the shared handheld lists use display casing, placeholders and UI-specific zero labels rather than separately ratified exact-control strings. The French resource also contains English `Lava Cookie` and `Devon Scope` entries. Runtime replacement of item 175 is explicit in the source, not proof of these controls' dynamic semantics.

[Bulbapedia's multilingual item list](https://bulbapedia.bulbagarden.net/wiki/List_of_items_in_other_languages) groups modern names by debut generation; it is not a complete version-specific Gen III oracle. Neither source is promoted unchanged to expected-name authority. This is a bounded search result, not a host-wide or web-wide absence claim. No expected labels were manufactured by decoding the captured records.

The first public-resource download stopped before retaining files because an English file has a UTF-16 BOM. Its failed script/log/receipt remain retained. The encoding-aware public download then completed all eleven files; it did not access original ROMs.

## Implementation boundary

Task 411 adds generic immediate-MUL getter forms and the complete simple wrapper to the previously shift-only getter and dynamic-wrapper-only recognizer. A nullable exclusion represents ordinary wrappers without a special branch; synthetic item ID zero remains available, while the dynamic contract still excludes its special ID. Existing candidate-count, original-route, scalar-inventory, extent, copier, ambiguity, cancellation and work-budget gates remain mandatory.

Nomination must not be tightened into a full-proof filter that silently drops incomplete competing candidates. The incomplete Ruby field-20 consumer, partial alternate callers and Spanish literal-overlap cases receive no new authority from this correction. Synthetic supported fixtures are not acceptance of the fifteen real controls. Complete consumer/reference proof and independent exact expected names must precede the separate production materialize → SQLite write/close/reopen → cache-only API gate.

## Task 411 initial focused execution

The initial retained execution is independently reconciled against **69 unique artifact pins**, all eight production/test source files at handoff, the five unchanged production files at RED, exact test selectors and JUnit XML:

- Initial offline Gradle setup stopped before tests: **0 executed**, exit 1. The read-only cache path was one directory too deep; that preparation failure remains separate from assertion evidence.
- Corrected RED: **5 executed / 5 assertion failures**, exit 1, before production edits. One unselected negative fixture was corrected during configuration; both test-source states and the correction note are retained, rather than claiming a wholly frozen RED test file.
- Frozen GREEN: **37 passed / 0 failures / 0 skipped**, exit 0, **71.954 seconds**. This comprises 35 resolver methods (21 retained and 14 added), one materializer method and one actual SQLite persistence method using synthetic catalogs.
- Parser revision is **61**; storage schema **2** and codec versions **1** are unchanged. The SQLite gate rejects revision 60 and closes/reopens revision 61 with a synthetic localized item overlay intact.
- Final source and executable patch hashes match the GREEN snapshots. Whitespace validation passed. Retained process evidence records no remaining owned Gradle clients, daemons or workers.

A final evidence-script shell-quoting failure was preserved and corrected without executing Gradle again. The parent read the runner, logs and correction records, then reconciled hashes and XML without rerunning any successful test gate. These tests establish synthetic recognizer/cache behavior only, not any of the 1,660 required official names.

### Task 412 review corrections — verified

The bounded code review identified and Task 412 corrected two gaps outside the initial passing selectors:

1. A distinct-root old-shift or MUL getter whose post-literal ADD/POP/BX was malformed could be omitted from nomination, allowing only the intact competitor to reach proof. Nomination now uses the conservative pre-literal prefix and root LDR; full tail proof remains in the resolver. Cross-root mutations cover all three tail instructions, both forms, hint caps 1/64, reference-site caps 1/16 and both original routes. Counts and malformed candidates remain visible; the nonpointer unsigned-return decoy remains unhinted.
2. Twelve synthetic `CatalogStoreTest` methods unconditionally expected current parser revision 60. Only those current-version assertions changed to 61; historical stale input versions are byte-for-byte unchanged.

The new frozen RED executed **14 JUnit methods / 14 assertion failures**, with no skipped, missing or unexpected methods. Two accumulating matrix methods recorded **48 incorrect Available outcomes and 24 lost-hint inventories**; twelve storage failures reported expected 60 / actual 61. Those matrix counts are separate from the JUnit denominator.

Corrected-source GREEN executed **51 methods / 51 passes / 0 failures / 0 skipped**, exit 0, **47.410 seconds**: 37 resolver methods, one materializer method and thirteen SQLite methods, including stale-60 rejection/current-61 reopen. Both gates retained frozen sources. Parent review reconciled **64 unique artifact pins**, exact XML selectors and failures, all eight final source files and the three-file correction delta; it verified that the complete resolver, materializer, models and revision constant were unchanged from Task 411. The bounded findings are closed for this checkpoint. No successful Gradle gate was rerun for reassurance.

Private evidence-only preparation corrections are retained: the owner audit needed explicit UTF-8, and the parent reconciliation needed LF/CRLF normalization for the textual delta plus JUnit's exception prefix in expected failure messages. Raw source/artifact and combined executable patch hashes remain exact. These corrections changed no executable repository source and caused no test rerun. Owned-process evidence records zero remaining Gradle clients, daemons or workers.

Tasks 411–412 provide a generic, synthetic-tested implementation checkpoint under `LNG-B002`, not official-name or Stage 4 closure. Initial Task 411 evidence remains immutable. No new original capture or broad corpus execution is authorized.

## Fork reconciliation

The pre-commit fetch found `fork/master` at `2fef59df95f6deb0ae861aad42cff52eff739a86`, already an ancestor of checkpoint base `39650c7d565e253c25c9f80ddc72c851f4cfe480` (54 ahead, zero behind). The remote feature branch matched that base. No incoming delta, merge, reset or force-push was needed; the tested executable sources remain unchanged.

## Evidence pins

| Artifact | SHA-256 |
|---|---|
| Task 405 preparation parent review | `b5393edb97a8675ca0bb899f0b1535ac42e25e33802059d4b1831e589b9ecc36` |
| Task 406 supplemental decision | `f27ae6118baf37e4627692c99209158f7ce39e64369667e9b991ef2096777e63` |
| Task 406 independent capture reconciliation | `660bd1fe968807ad09b27ddc08565adf66bd2cd7f38176885700d4bbdcacd618` |
| Task 407 owner handoff | `a2e2c1d888f4c69f94e208f778f0ad428abe6017f04a890cfd623fb9cbc8be5b` |
| Task 407 independent byte review | `24cd17ff12592d333620a6ecf379a6fff5b9620e33b86c39a62fdd64fc4ab74e` |
| Task 407 parent review | `5fb99f2b8792fe35f864d683e75d55c0d013b28374e8f8f7865a3d1c03449783` |
| Task 408 parent token review | `7dd68f9ff7d93ba20ed5a52d6ee46cb8e27e1690c2ca6d0aa1ed7ddaff1e00c7` |
| Task 410 public-reference assessment | `2fa0e6a400a3b62ae0e45856f3f7416bb0b12752bc270bc9d407df4bf7cb20dc` |
| Task 411 implementation handoff | `391ccb0af8d8be21b4cbe46ec85d108e2bf3dcb10027f2694218008add892676` |
| Task 411 frozen executable patch | `a707a8f22929951345864566ebb9213c37ccf5af51489546e3813b7327bf212c` |
| Task 411 parent execution/source reconciliation | `09ce01d84822764442a417e4033209efc530fc66ac04c335554e6c0713baf082` |
| Task 412 corrected implementation handoff | `7d8cbda5e69701e0630dd8c5b46d5084b3550a0c0855b9857239f20782c9b166` |
| Task 412 combined executable patch | `e197ed1a5d2cd09b1623d1a164fa44f8616369a67ec453705334312b5b9f9337` |
| Task 412 parent correction/execution reconciliation | `18ce9d0ad895090da49bc04d6530878afda5946752c3bf4f99216e6efa82da2e` |

**Owner/target/acceptance:** Task 390 / `LNG-B002` retains all **1,660 mandatory names**. Task 386 retains Local/region-label and reference-aware POI gaps. Tasks 373–375 retain all fifteen capability dispositions, the current **43-cell/44-control** matrix, final current corpus after executable changes stabilize, audited ledger and published Stage 4 closure. No required cell is deferred; Stages 5–6 remain blocked. Raw windows, original paths and private payloads remain outside public assets. No corpus, device or signing work is part of this diagnostic checkpoint.
