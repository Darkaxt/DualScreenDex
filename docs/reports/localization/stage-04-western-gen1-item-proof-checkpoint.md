# Western Gen I referenced-item proof stopping boundary

## Status and scope

Task 393 under Task 390 / `LNG-B002` remains **open**. The first exact ten-control independent proof exited **2**, `PARTIAL_OR_UNSUPPORTED`. No independently derived item label was accepted. This does not supersede the earlier [diagnostic](stage-04-western-gen1-item-name-diagnostic.md), certify its 616 observed names, or establish the cause of the two French missing-name gaps.

The scope is the five official Western languages, each with one exact Red/Blue-family and one Yellow control from the retained 35-control inventory. All **620 requested-ID instances** and **1,585 original typed references/numeric item POIs** remain accounted for. Production remains the `5d65bf72` checkpoint, parser **59**, storage **2**; the published diagnostic checkpoint is `672f7f70`. This report introduces no production or test-code changes.

## Independent source policy preparation

A separate source-only manifest describes all **2,560 byte entries across ten dialect views**, backed by **26 pinned sources**. Independent exhaustive review checked 1,100 admitted glyph/contraction mappings, 514 main-font declarations and 4,889 citations. The coordinator's fresh source-only gate passed **24/24**, zero failures/errors/skips, without ROM reads or proof execution.

Exactly **60 source-supported fixed policies** were separately ratified for ordinary referenced-item proof **only after binding the outer consumer and particular input**. The frozen manifest itself still marks those entries as requiring ratification. This is not a production/global-decoder policy, generated-prefix permission, borrowed Gen II authority, or execution authorization. Dynamic/contextual substitutions remain denied. Ambiguous Pokémon forms `4A/E1/E2` remain denied; this does not exclude the 90 source-supported apostrophe-contraction entries.

The rejected Italian source candidate supplies no fallback. Localized consumer-source gaps do not establish absent compiled behavior. Two-byte generated prefixes remain identified by branch and compiled root, never by their equal widths. No French token-cause conclusion follows from source policy preparation.

## Checker corrections and fabricated verification

The first frozen checker passed its owner's 62 fabricated tests, but independent review reproduced three false acceptances:

1. A hidden handler's literal store overwrote the item argument before the wrapper call, while the checker still derived the original item.
2. A pointer scratch operand could address read-only I/O rather than writable HRAM.
3. Non-overlapping nomination skipped a complete competing wrapper starting inside another candidate.

The original checker and all four reviewer inputs/results remain immutable. A new private revision reproduced **three expected RED failures** against the pinned old checker, with complete outputs equal to the reviewer's originals. The corrections bind the hidden literal destination and per-reference input trace, require genuine `FF80–FFFE` HRAM roles with distinct storage, and preserve overlapping candidates through one bounded, capped lookahead pass. No output-based consumer selection or whole-table prerequisite was added.

After focused 7/7, 7/7 and 4/4 gates, the owner's single combined revision gate passed **80/80: 50 unchanged checker tests, 12 unchanged pure-wrapper tests and 18 revision regressions**, zero failures/errors/skips. Independent retained-only review reconciled all 36 flat artifacts, 49 archived copies/current originals, 76 metadata pins, ten bundles, RED/GREEN inputs and complete AST/log/XML/receipt inventories. The 18 retained revision cases include five supported positives and thirteen expected rejections; their 1,177 raw windows match the fabricated inputs.

The coordinator reviewed the concrete code delta and ran the exact combined gate once in a new private copy: **80/80**, exit **0**, zero failures/errors/skips, **0.627 seconds** process time. All copied/frozen hashes remained unchanged; all 18 complete outputs matched the owner's retained results, and all three original counterexamples were rejected. Synthetic success did not establish compatibility with the exact controls.

Historical failures are not relabeled: the owner's original 62-test gate missed the later counterexamples; two post-gate shell/encoding packaging failures were not suite failures; an earlier coordinator metadata verifier required CRLF normalization when matching retained log lines. No final gate was rerun to conceal these outcomes.

## Single exact-control execution

Only after the code, policy, retained-evidence and fresh fabricated gates passed did the coordinator invoke the explicit-opt-in wrapper once. Exclusive one-shot/worker markers remain in place. The **300-second** watchdog did not fire; wrapper elapsed time was **2.064 seconds**, with **one exact-size/SHA integrity read per control**, no automatic retry and no parser/materializer invocation.

Every control independently nominated one item wrapper and one hidden handler and derived naming mechanics consistent with the original authority. Reference validation then stopped before packed-name traversal, generated-name evaluation or token decoding:

| Each control in family | Requested IDs | Original references | Reference-scoped membership | Unproved hidden references | Terminal reference condition |
|---|---:|---:|---:|---:|---|
| Red/Blue, each of five languages | 62 | 156 | 104 | 52 | `HIDDEN_STATE_ALIAS` |
| Yellow, each of five languages | 62 | 161 | 108 | 53 | `HIDDEN_STATE_ALIAS` |
| Total | 620 | 1,585 | 1,060 | 525 | No control completes the reference gate |

The downstream missing-reference diagnostics include IDs 3/68 for Red/Blue and 3/11/68/79 for Yellow. These are **proof-gate dispositions**, not new production missing-name observations. All requested fields remain unavailable in this proof, all derived/accepted label maps are empty, and every control retains `semanticAcceptance=false` / `requiredSemanticCompletion=NOT_ACCEPTED`.

Coordinator retained-only reconciliation checked all ten result files against the batch and original bundles, exact requested/reference domains, read counters and unchanged frozen artifacts. All **30,040 retained windows** have matching lengths/digests and nonconflicting overlapping bytes. This verifies retained-evidence consistency; it is not a second original-ROM read or independent whole-input nomination replay. The worker log is empty, and the recorded exit is the unsupported reference outcome rather than a timeout or launch failure.

## Subsequent source-backed lifetime and retained-membership check

Pinned Red/Blue and Yellow RAM declarations explicitly assign the coordinate-X and selected-index names to one byte. The complete read-only leaf captures X into a CPU register before returning the first-match index; the caller then stores and reloads that index. A new private checker revision permits only this operand-relative **X → index** ownership transfer, alongside the existing separate-slot form. Other unratified overlaps remain unsupported. Exact leaf/call/store/reload order, distinct remaining state roles, literal-store nonaliasing and actual item-argument binding remain required; no fixed-address whitelist or general RAM/callee analysis was introduced.

Five fabricated shared-slot cases failed against the pinned predecessor and passed unchanged after correction. Focused gates passed **20 lifetime and 16 supplement tests**, following one expected supplement-scaffold failure. The single owner combined gate passed **116/116**. Independent retained-only review reconciled **32 handoff artifacts, 115 external pins and 85 unchanged predecessor-indexed artifacts**, both new RAM-source pins, all original domains, RED/GREEN inputs and complete method inventories. The coordinator separately ran the frozen combined gate in a fresh private copy: **116/116**, exit **0**, zero failures/errors/skips, **1.121 seconds** process time. Complete retained fabricated outputs match the owner's; copied and frozen inputs remained unchanged.

After those reviews, the coordinator separately authorized and executed **one retained-only membership supplement**: exit **0**, **1.483 seconds** process time (**0.952 seconds** reported evaluation), no timeout or retry. It combines previously retained code/event windows with the original diagnostic's pinned coordinate-prefix witnesses. Missing or conflicting bytes fail closed; no absent byte is zero-filled. It performs **zero original-ROM reads, zero new nomination scans and zero naming/token evaluation**. The original failed full-input nomination and outer-consumer record are explicitly historical dependencies, not newly established sparse-input uniqueness.

All **1,585 references** now pass this supplemental membership boundary: **1,060 visible and 525 hidden**. Every one of the **620 requested-ID instances** has a refreshed reference, but every name field remains **unavailable/null**, with empty derived/accepted labels and no static-name or semantic acceptance. Coordinator reconciliation checked all ten result files, all **29,230 refreshed windows** against the merged retained bytes, and every hidden reference's fresh coordinate/index and argument-to-wrapper traces. The earlier failed batch and all frozen inputs remain unchanged.

Two coordinator reconciliation failures are retained separately: an inline shell launch failed before Python execution, then a verifier assumed nomination-derived candidate counters existed in the no-scan supplement. Moving the verifier into a file and asserting the actual absent-counter schema resolved those verification-only failures. The supplement itself was not retried.

## Remaining blocker and acceptance

**Owner / target:** Stage 4 Task 393 — bounded missing-name evidence acquisition and independent name proof, then Task 390 — mandatory Western Gen I semantic acceptance.

The hidden-state lifetime stopping condition is resolved for the separately checked retained-reference membership scope above, not for names or production behavior. Ordinary-root bytes and both generated-prefix payloads remain absent from the audited retained evidence. A separate acquisition package is being prepared without execution; its contract, input pins and execution scope require review before any new original read. Sparse evidence cannot establish the missing naming payloads or fresh whole-input nomination.

**Required acceptance:** preserve all rejected unsafe cases, exact current-reference/input binding and existing bounds, and obtain independently authorized names through the original producer, exact projection, actual SQLite close/reopen and cache-only API. The French IDs 46/68 remain mandatory and their production failure cause remains unknown. The original failed one-shot result is never retried or rewritten; the successful membership supplement does not relabel it.

**Temporary disposition:** no Gen I policy or production change; missing/unproved labels remain unavailable and counted. Task 390's Western GBA work, Task 386's remaining Local/region-label and reference-aware POI gaps, and Tasks 373–375's current 43-cell/44-control matrix, all fifteen capability dispositions, final current corpus after executable changes stabilize and published closure remain mandatory. Stage 4 stays open; Stages 5–6 stay blocked. No corpus, native acceptance rerun, device, signing, APK, cleanup or ROM publication occurred.

## Private evidence identifiers

Only digests are published; exact paths, input payloads and retained raw windows remain outside public assets.

| Artifact | SHA-256 |
|---|---|
| Source-token manifest | `13e201bfd6bb359857fb6f6ddb378fc48e18b7d72d97e83227f8b5663af4bee2` |
| Coordinator source-only 24-test receipt | `34f2daea53ac880e274d2b4def549261b947fd3da23121f9995eb2b5ae0c1271` |
| Ordinary-only source policy review | `2e860c53ca9c145279f4250e3bef6d2a727ee9661d60078299e1fe4e40c18f61` |
| Corrected frozen handoff | `b193b9cfd7aac99760f0f601cf2e80cfb1444cc7034d70a7f63ebee3e0edebef` |
| Corrected checker | `3ff53942a796f8718ac5be80f811f0f3bebb478b5476ce69d17d2686307adfbd` |
| Owner final revision receipt | `fd8cdbe522df31998c5ce329676a9cfe0b200a8f4dd09862e1ecc009475cacea` |
| Coordinator fresh 80-test receipt | `af223c7ecd17f7303d0606da17eaab16979178bc150d3fec261b30667fe13336` |
| Failed exact-control batch | `494dce9b2fffc4dbaaf95037781039c95ed166b9a7e53c6c4659ec3da7566cba` |
| Coordinator retained-result reconciliation | `98ba3d40ee82a624ecc3ad7f4e859ba3424e6dcca0a4b510cf2b02bc2863ed1b` |
| Red/Blue RAM source | `00683cd430a9ef30794d2aeaee51f3a5440e4bb14dca13e9f9cb5fa88f130e34` |
| Yellow RAM source | `dc69e9c8a6fe8f6262abcc5c5d6ccdd6cf8c8b3e6b76aa635be8590b3998e4ce` |
| Lifetime revision handoff | `df5c214430a50daee9ea75fe40d50099916346708bf73adbec3787496e604221` |
| Lifetime checker | `0f887a631352d3a181bcdbb12d614bc90d5d229c1cadb9664e7098c3feb3d9fd` |
| Coordinator fresh 116-test receipt | `a95f7622d401eaed6c153f009298fdc1cc2b395030a695629a85892581aa4cc0` |
| Retained-only membership batch | `a04544e92616086628b803884f39860b8e11ae9a9bed305f48550bda30d94157` |
| Coordinator membership reconciliation | `1c5ddec2637d68aa514542ea3c28f982a1a9b957c48788eccb22b67cefe9892f` |
