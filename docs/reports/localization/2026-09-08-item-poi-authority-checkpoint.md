# Localization item and POI authority checkpoint

## Status

**Partial semantic checkpoint; Stage 4 remains open.** The changed-source fifteen-control run now preserves all **1,660 requested item IDs**, with **1,357 independently exact names across twelve individually complete controls**. French, Italian and Spanish Ruby each remain **0/101**. The aggregate real-control method failed: batch `semanticAcceptance=false`, `acceptedNames=0`, `diagnosticComplete=false`. This supersedes Task 413's latest-observation status, not its retained historical 0/1,660 result.

This checkpoint belongs to Tasks 414–420 and ledger blockers **LNG-B002/LNG-B003**. Task 418 is integrated: parser revision **62** invalidates prior cached text semantics; SQL schema **2** and codec revisions are unchanged. The final matrix/corpus, Stages 5–6, device testing, signing and release have not run.

## Compiled item-consumer corrections — Task 414

Task 413 failed before semantic materialization because the broad original-root nomination inventory included complete consumers that were not direct inline-name getters. Nomination remains deliberately broad: damaged competing getters must reach validation rather than disappear.

The corrected resolver distinguishes:

1. **Complete distinct local wrappers.** The conditional u8-indexed call wrapper and category-copy wrapper can discharge only their original direct-getter hints. They do not authorize item roots or establish opaque callee semantics. Same-root references still require independent reconciliation.
2. **Exact getter-owned literal words.** Spanish FireRed has nineteen root-reference interpretations: fourteen getters and five literal words misread as Thumb instructions. Only a complete recognized getter with a closed return and exact aligned four-byte pool can establish ownership. The existing bounded caller scan guards both literal halfwords and any positively established zero alignment halfword against supported Thumb BL/B/Bcc incoming edges. Counts, caps, recovery and malformed competitors remain enforced. This is not a global claim about ARM or computed execution.
3. **A separate R1-input description consumer.** Ruby has an additional complete 96-byte local description-line consumer. Its sole call must resolve to the complete u16 sanitizer. Its root, stride, count and description-field offset must agree with the independently validated item ABI. It never replaces or becomes an additional scalar getter; all thirteen scalar witnesses remain mandatory. No description payload or destination memory is traversed.

Generated tests use relocated hand-assembled fixtures, not retained ROM blobs. They cover complete positives, every required description-consumer code halfword, geometry and sanitizer mismatches, missing scalar witnesses, escaped literals, incoming branches, budget exhaustion and cancellation.

### Retained focused results

| Gate | Tests | Failures | Errors / skips |
|---|---:|---:|---:|
| Initial local-wrapper RED | 47 | 3 expected | 0 / 0 |
| Local-wrapper GREEN | 49 | 0 | 0 / 0 |
| Literal-pool RED | 55 | 4 expected | 0 / 0 |
| Literal-pool GREEN | 57 | 0 | 0 / 0 |
| Alignment-padding RED | 58 | 1 expected | 0 / 0 |
| Alignment-padding GREEN | 58 | 0 | 0 / 0 |
| Ruby description-consumer RED | 64 | 2 expected | 0 / 0 |
| Final item-consumer GREEN | 64 | 0 | 0 / 0 |

The final item suite contains 60 resolver tests and four reference-index tests. Parent review reconciled actual retained XML, build outcomes and stable before/after source pins, and retained an exact final source snapshot. These are synthetic results, not actual-ROM availability or semantic acceptance.

## Independent expected names — Task 416

The complete private fixture preserves the fifteen exact control identities and all **1,660** requested IDs:

- **1,045** literal names from pinned original-version public source tables, bound to the controls through nine same-input SHA1/SHA256 checks.
- **312** German/Italian/Spanish Emerald names independently evaluated from previously retained compiled field declarations and static records.
- **303** French/Italian/Spanish Ruby names independently evaluated from newly acquired bounded name/self-ID records.

Static evaluation does not import the production decoder or use production observations as expectations. The finite proof binds the source-declared 14-byte name and two-byte self-ID fields to retained pointer, sanitizer and scalar consumers. The single-glyph public Western character map excludes dynamic, ambiguous and unsupported tokens. Every requested domain excludes dynamic Enigma item 175.

Comparison is exact display-string equality after ASCII whitespace collapse/trim only. There is no case, accent or Unicode normalization and no unrestricted semantic-equivalence matching. English-looking labels are not automatically wrong in a localized source table.

### Acquisition and provenance

After reading the reader, evaluator, preparation/finalization code and safety tests, the parent reran **25/25** synthetic/retained-only checks and reconciled the exact input/domain/window pins. A separate coordinator decision under the approved host plan enabled one new acquisition:

- Twelve exact inputs, each streamed once: **201,326,592 bytes** total.
- Nine source-version identity bindings.
- 303 Ruby records × 16 bytes: **4,848 bytes** retained.
- Ten previously missing Ruby code spans: **130 bytes** retained.
- **4,978 bytes** retained in total; no Emerald records reacquired.
- No historical one-shot collector reused and no automatic retry.

The retained-only finalizer produced **1,660 independently ready expectations**. Fixture SHA256:

`3de6d6c19ab7212674a0dda32c31969bb788ef8f7b0035fd46486447062d7a47`

Raw captures, original paths and the private proof fixture are not public assets. Ready expectations do not themselves establish production semantic acceptance.

### App comparison and executed partial acceptance

The existing diagnostic selector now supports an additional explicit semantic opt-in in the **same single capture**. External fixture/SHA binding validates all fifteen identities and exact domains before output reservation or original access. Independent names are compared at the producer, materialized overlay/projection, freshly reopened SQLite overlay/projection and every cache-only API ball/POI occurrence. Diagnostic, numeric/domain, language, integrity and original-authority checks remain mandatory. FireRed's requested ID zero remains required; unavailable names and presentation fallbacks cannot count as accepted text.

| App gate | Methods | Failures | Errors / skips |
|---|---:|---:|---:|
| Compiling behavioral RED | 36 | 10 expected | 0 / 0 |
| Final synthetic GREEN | 36 | 0 | 0 / 0 |
| Combined gate: 36 synthetics plus one fifteen-control real method | 37 | 1 real-method failure | 0 / 0 |

The synthetic suite retains all 23 diagnostic methods plus eight oracle and five semantic-capture methods, including actual synthetic SQLite/cache-only startup. Intermediate missing-import, fixture-obligation/coverage and immutable-fixture construction failures remain retained; they are not substituted for behavioral RED or final GREEN.

The actual combined gate took **1,178.25 seconds**, with zero timeouts, missing/unexpected methods, duplicates or source changes. It performed **fifteen original reads**, exactly one analysis and item-producer invocation per control, preserved every requested domain and **4,470 API item occurrences**, and recorded **zero API reparses**.

| Controls | Requested names per control | Available and independently exact per control | Individual terminal |
|---|---:|---:|---|
| English/German Ruby | 101 | 101 | `SEMANTIC_ACCEPTANCE_COMPLETE` |
| All five Western Emerald controls | 104 | 104 | `SEMANTIC_ACCEPTANCE_COMPLETE` |
| All five Western FireRed controls | 127 | 127 | `SEMANTIC_ACCEPTANCE_COMPLETE` |
| French/Italian/Spanish Ruby | 101 | 0 | `SEMANTIC_ACCEPTANCE_FAILED` |

Each of the twelve complete controls passes all six independently checked boundaries. Their **1,357 names** are a verified partial result, **not an aggregate gate pass**. Each remaining Ruby control retains all 101 requests and thirteen semantic check failures. Its selected authority reports `Unavailable("missing or conflicting static item copy contracts")`; the retained-only Task 414 owner investigation is open. This stopping reason is not an original-getter or newly added description-consumer recognition failure, and no correction is claimed here.

Parent retained-only reconciliation checked the final XML, all **795 current source pins**, and the twelve complete control boundaries. Execution-result SHA256: `9230f61068422d27cc0c2a77481815bc844992839dd022a42c89d3553abff35b`. Batch acceptance remains false with zero batch-accepted names; no failing control or required ID is waived.

Current-produced native SQLite metadata assertions now use the current schema constants while preserving storage revision 2 and historical stale-cache inputs. **No native original selector was rerun.**

## Reference-aware POI coverage — Task 415

POI coverage previously counted only duplicated direct text while expecting every POI. Item and destination references therefore could not satisfy their own label obligations.

Shared POI models now retain an explicit producer-established obligation: `DIRECT_TEXT`, `GENDERED_DIRECT_TEXT`, `ITEM_NAME`, `DESTINATION_NAME`, or `UNRESOLVED`. All three generation producers assign obligations independently of whether decoding succeeds. Unresolved events remain in the expected denominator.

A shared resolver drives extraction coverage, overlay graph validation and selected-language projection. Item and destination references use only the selected overlay, without duplicating strings. Destination joins require the unique Local-map base-area binding. A direct sign cannot borrow a nearby destination label; required gender variants must both exist. Area Guide silhouette/knowledge gating, numeric identities, coordinates, media and geometry remain intact.

Three behavioral RED boundaries were retained: item-reference coverage, old overlay coverage validation, and missing destination projection. The focused GREEN evidence contains **45 passing tests**: parser 32, actual SQLite store one, and Area Guide twelve; zero failures/errors/skips. The unchanged Area Guide test task reused its immediately preceding successful execution in the final batch.

The SQLite test writes, closes, freshly reopens and compares Japanese/Korean projections. It also rejects missing/null/unknown persisted obligations and a changed destination binding with stale claimed coverage.

## Region authority and cache invalidation — Tasks 417–418

Gen II previously emitted literal English `Johto`/`Kanto` titles merely because a codec and region bindings existed. The minimal correction removes those fabricated titles while preserving region keys, numeric bindings, geometry and rasters. English/Japanese/Korean generated controls and relocated/unreferenced-decoy cases cover the correction.

Region-related host evidence: three expected RED failures; GREEN **32 passes and one existing opt-in skip**, no failures/errors. This removal does not prove contextual Local-name exclusions or Gen III region-title applicability; those targets remain open.

Parser revision 62 rejects revision 61 metadata before otherwise valid sections can bootstrap through either a fresh SQLite reader or the cache. Fresh revision 62 rewrite/reopen succeeds, including the shared POI obligations. The focused cache gate had **two expected RED failures**, followed by **24/24 GREEN**, zero errors/skips. Historical stale revision inputs are unchanged. Parent reviewed the incremental patch and matched both integrated files to the tested isolated sources.

### Task 417 v2 executed diagnostic boundary

The separately scoped v2 acquisition executed **seven original reads**, exit **2**, in **7.06 seconds**, with no timeout or automatic retry. Four Gen II controls retained diagnostic captures of **215/269/215/215 bytes**. Three GBA controls stopped with **zero new bytes** and `PARTIAL_STOP_NO_EXPANSION`. This is executed diagnostic evidence, not title authority, contextual exclusion or sign acceptance. The captured three-byte Japanese Crystal script begins with opcode **`0x52`, not `0x53`**; its text operand was not dereferenced.

Task 419 now owns the bounded **GBA region-title nomination** follow-up. Task 420 owns the **Japanese Crystal declared-sign grammar** follow-up. Both require independently bound compiled authority before any positive labels or applicability closure. Task 417's contextual Gen II Local-name exclusions and native/Western region applicability remain open.

## Remaining acceptance

- Task 414 / Task 390: resolve the three retained Ruby static-copy-contract failures, then obtain separately reviewed changed-source semantic acceptance for all mandatory controls. Keep all 1,660 requested IDs and the twelve-control positive evidence; do not relabel the failed aggregate gate or add a redundant producer invocation/reparse.
- Task 417: prove contextual Gen II Local-name exclusions and remaining region-title applicability. Task 419: establish bounded GBA title nomination and its required producer/projection/reopen/API authority or justified applicability disposition.
- Task 420 / Task 386-D: bind Japanese Crystal `local/1804/bg/0` to its declared script/text grammar and prove required headlines through reopen/API. The retained three-byte script beginning with `0x52` is not dereferenced text proof. Remaining direct-sign/service obligations cannot be waived by reference-aware coverage.
- Tasks 373–375: ratify the current **43-cell/44-control** matrix with all fifteen capability dispositions and current native evidence, then run the final source-bound corpus once executable changes stabilize. Native metadata assertions are corrected, but current native original acceptance has not been rerun here.
- Publish truthful Stage 4 closure before beginning Stages 5–6.

No mandatory content is waived by unavailable output, `NOT_FOUND`, synthetic success, diagnostic-only capture or twelve-control partial acceptance. No final matrix/corpus, Stages 5–6, device, signing or release execution is included.
