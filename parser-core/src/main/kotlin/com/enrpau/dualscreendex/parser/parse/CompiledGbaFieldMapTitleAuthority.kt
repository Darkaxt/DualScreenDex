package com.enrpau.dualscreendex.parser.parse

/** Independently proven flow from a static title source into a window renderer. */
internal interface CompiledGbaTitleWindowAuthority

/** Complete owner flow binding a title declaration to the selected field-map loader. */
internal interface CompiledGbaFieldMapOwnerAuthority
